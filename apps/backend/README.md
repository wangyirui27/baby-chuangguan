# apps/backend

Enhanced Express backend for 宝宝英语岛.
The current live backend code lives at `/backend/` (root level, preserved in-place).

This directory is the target for future worktree-based development.
See: `packages/contracts/` for the API contract.
