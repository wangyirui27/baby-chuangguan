# ─── 豆包 TTS 配置 ───────────────────────────────────────
# 所有值从火山引擎控制台获取：https://console.volcengine.com/speech/app
# ⚠️ 严禁在代码、日志、commit message 中出现真实凭据

# [必填] 应用 ID
DOUBAO_APP_ID=your_app_id_here

# [必填] Access Token — 仅用于 Authorization: Bearer;{token}
DOUBAO_TOKEN=your_access_token_here

# [必填] 业务集群 — 标准音色用 volcano_tts，大模型音色用 volcano_mega
DOUBAO_CLUSTER=volcano_tts

# [兼容旧脚本] 音色 ID — 当前生成器在代码中固定：海岛 Natasha / 沙漠 Hayley
DOUBAO_VOICE_TYPE=en_female_natasha_uranus_bigtts

# [可选] 采样率，默认 24000
DOUBAO_SAMPLE_RATE=24000

# [可选] 音频格式，默认 mp3
DOUBAO_AUDIO_FORMAT=mp3

# ─── 短信（认证登录）───────────────────────────────────
# development | aliyun
SMS_PROVIDER=development
# 生产/真短信：SMS_PROVIDER=aliyun，密钥与签名复用影关（movie-game）阿里云 Dysms：墨斗曲线 + 验证码模板

# 阿里云（SMS_PROVIDER=aliyun 时必填）
# SMS_ALIYUN_ACCESS_KEY_ID=
# SMS_ALIYUN_ACCESS_KEY_SECRET=
# SMS_ALIYUN_SIGN_NAME=
# SMS_ALIYUN_TEMPLATE_CODE=
# SMS_ALIYUN_TEMPLATE_PARAM_KEY=code

# ─── Auth 存储（登录 / session / 验证码）────────────────
# 默认 json → 仓库根 data/*.json（单机）
# 切 RDS：AUTH_REPOSITORY=mysql，并填齐下方 MYSQL_*（缺一不可，无 localhost/root 默认）
# AUTH_REPOSITORY=json
# AUTH_REPOSITORY=mysql
# AUTH_FORCE_JSON=1

# ─── Learning 仓库（进度同步）───────────────────────────
# 生产推荐显式指定，勿依赖默认探测：
# LEARNING_REPOSITORY=insforge
# LEARNING_REPOSITORY=mysql
# LEARNING_REPOSITORY=none
# 别名：LEARNING_BACKEND=mysql|insforge|none

# ─── InsForge（LEARNING_REPOSITORY=insforge 时必填）─────
# INSFORGE_URL=https://your-appkey.region.insforge.app
# INSFORGE_API_KEY=server_only_project_api_key
# 兼容旧名（二选一即可）：INSFORGE_SERVICE_KEY=

# ─── MySQL / RDS（AUTH 或 LEARNING 任一为 mysql 时必填）─
# 本机现状：backend/.env 通常指向阿里云 RDS（MYSQL_HOST=rm-….rds.aliyuncs.com），
# 不是本机 mysqld。改连本机库时把 HOST 换成 127.0.0.1 并建好库。
# 代码不再回落 127.0.0.1 / root / baby_island；必须写全：
# MYSQL_HOST=rm-xxxxxxxx.mysql.rds.aliyuncs.com
# MYSQL_PORT=3306
# MYSQL_USER=
# MYSQL_PASSWORD=
# MYSQL_DATABASE=baobao_chuangguan
# MYSQL_CONNECTION_LIMIT=5

# Admin console (/admin) bearer token
ADMIN_TOKEN=

# 视频 CDN / OSS 公网前缀（admin 关卡目录；也可只在 content-catalog 里配）
OSS_PUBLIC_BASE_URL=
# COURSE_VIDEO_BASE=
